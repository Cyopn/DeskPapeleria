<Window
    x:Class="DeskApp.TransactionDetailsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:DeskApp"
    Title="Detalle de transaccion"
    Width="980"
    Height="680"
    ResizeMode="CanResize"
    WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <local:SpecialServiceTypeDisplayConverter x:Key="SpecialServiceTypeDisplayConverter" />
        <Style x:Key="DataGridCellTextStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8">
                            <ContentPresenter
                                Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#FFE5E5E5" />
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#FFBDBDBD" />
                                <Setter Property="Foreground" Value="#FF8C8C8C" />
                                <Setter Property="Opacity" Value="0.75" />
                                <Setter Property="Cursor" Value="Arrow" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,10"
            FontSize="24"
            FontWeight="Bold"
            Text="Detalle de transaccion" />

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock
                    Grid.Row="0"
                    Margin="0,0,0,4"
                    FontSize="16"
                    FontWeight="SemiBold"
                    Text="{Binding HeaderLine}" />
                <TextBlock
                    Grid.Row="1"
                    Margin="0,0,0,4"
                    FontSize="14"
                    Text="{Binding MetaLine}" />
                <TextBlock
                    Grid.Row="2"
                    FontSize="14"
                    Text="{Binding FooterLine}" />
            </Grid>

            <Border
                Grid.Column="1"
                Width="150"
                Height="150"
                Margin="12,0,0,0"
                Padding="8"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                BorderBrush="#DDDDDD"
                BorderThickness="1"
                CornerRadius="8">
                <Image x:Name="QrImage" Stretch="Uniform" />
            </Border>
        </Grid>

        <DataGrid
            x:Name="DetailsDataGrid"
            Grid.Row="2"
            Margin="0"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            AlternatingRowBackground="#FFF7F7F7"
            AutoGenerateColumns="False"
            BorderThickness="0"
            CanUserReorderColumns="False"
            CanUserResizeColumns="False"
            FontSize="20"
            FontWeight="DemiBold"
            GridLinesVisibility="None"
            HeadersVisibility="Column"
            IsReadOnly="True"
            ItemsSource="{Binding Items}"
            PreviewMouseLeftButtonUp="DetailsDataGrid_PreviewMouseLeftButtonUp"
            RowBackground="White">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="BorderThickness" Value="0,0,0,1" />
                    <Setter Property="BorderBrush" Value="Black" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Foreground" Value="Black" />
                    <Setter Property="OverridesDefaultStyle" Value="False" />
                    <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <ScaleTransform ScaleX="1" ScaleY="1" />
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#22FFD54F" />
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                            To="1.01"
                                            Duration="0:0:0.12" />
                                        <DoubleAnimation
                                            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                            To="1.01"
                                            Duration="0:0:0.12" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                            To="1"
                                            Duration="0:0:0.12" />
                                        <DoubleAnimation
                                            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                            To="1"
                                            Duration="0:0:0.12" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="Foreground" Value="Black" />
                        </Trigger>
                        <DataTrigger Binding="{Binding IsSpecialService}" Value="True">
                            <Setter Property="Cursor" Value="Hand" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Foreground" Value="Black" />
                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="Foreground" Value="Black" />
                            <Setter Property="BorderThickness" Value="0" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="BorderThickness" Value="0,0,0,1" />
                    <Setter Property="BorderBrush" Value="Black" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                    <Setter Property="Margin" Value="5,5,5,5" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="Transparent" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding DetailId}"
                    ElementStyle="{StaticResource DataGridCellTextStyle}"
                    Header="ID" />
                <DataGridTextColumn
                    Width="3*"
                    Binding="{Binding ProductName}"
                    ElementStyle="{StaticResource DataGridCellTextStyle}"
                    Header="Producto" />
                <DataGridTextColumn
                    Width="2*"
                    Binding="{Binding ProductType, Converter={StaticResource SpecialServiceTypeDisplayConverter}}"
                    ElementStyle="{StaticResource DataGridCellTextStyle}"
                    Header="Tipo" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Amount}"
                    ElementStyle="{StaticResource DataGridCellTextStyle}"
                    Header="Cantidad" />
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Price, StringFormat=C}"
                    ElementStyle="{StaticResource DataGridCellTextStyle}"
                    Header="Importe" />
                <DataGridTemplateColumn Width="*" Header="Archivo">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button
                                Width="90"
                                Height="28"
                                Margin="3"
                                Click="PreviewFileButton_Click"
                                Content="Ver PDF"
                                Cursor="Hand"
                                FontSize="14"
                                IsEnabled="{Binding IsPreviewAvailable}"
                                Tag="{Binding}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel
            Grid.Row="3"
            Margin="0,10,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                x:Name="PrintFilesButton"
                Width="170"
                Margin="0,0,10,0"
                Click="PrintFilesButton_Click"
                Content="Imprimir archivos"
                Cursor="Hand" />
            <Button
                x:Name="CompleteSpecialServiceButton"
                Width="220"
                Margin="0,0,10,0"
                Click="CompleteSpecialServiceButton_Click"
                Content="Completar s. especial"
                Cursor="Hand" />
            <Button
                x:Name="CompletePurchaseButton"
                Width="180"
                Margin="0,0,10,0"
                Click="CompletePurchaseButton_Click"
                Content="Completar compra"
                Cursor="Hand" />
            <Button
                Width="100"
                Click="CloseButton_Click"
                Content="Cerrar"
                Cursor="Hand" />
        </StackPanel>
    </Grid>
</Window>
