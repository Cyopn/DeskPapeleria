<Window x:Class="DeskApp.QrScanWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Escanear QR de transaccion"
        Width="720"
        Height="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Closing="Window_Closing">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Escaneo de QR" FontSize="22" FontWeight="Bold" Margin="0,0,0,10"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="CameraIpTextBox" Grid.Column="0" Height="34" VerticalContentAlignment="Center" Margin="0,0,8,0" FontSize="16"/>
            <Button Grid.Column="1" Content="Guardar IP" Width="110" Margin="0,0,8,0" Click="SaveCameraIpButton_Click" Cursor="Hand"/>
            <Button x:Name="ReadFromCameraButton" Grid.Column="2" Content="Iniciar lectura" Width="130" Click="ReadFromCameraButton_Click" Cursor="Hand"/>
        </Grid>

        <Border Grid.Row="2" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="8" Background="#F7F7F7" Padding="6">
            <Image x:Name="CameraPreviewImage" Stretch="Uniform"/>
        </Border>

        <TextBlock x:Name="StatusTextBlock" Grid.Row="3" Margin="0,8,0,8" Text="Esperando lectura..." FontSize="13" Foreground="#666"/>

        <TextBox x:Name="QrJsonTextBox"
                 Grid.Row="4"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"
                 FontFamily="Consolas"
                 FontSize="14"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Cancelar" Width="100" Margin="0,0,8,0" Click="CancelButton_Click" Cursor="Hand"/>
            <Button Content="Buscar transaccion" Width="170" Click="ScanButton_Click" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>