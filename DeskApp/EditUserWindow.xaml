<Window
    x:Class="DeskApp.EditUserWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Editar Empleado"
    Width="420"
    Height="480"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#D9D9D9" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="2,2" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8">
                            <ContentPresenter
                                Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="20,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock
            x:Name="WindowTitleTextBlock"
            Grid.Row="0"
            FontSize="24"
            FontWeight="Bold"
            Text="Editar empleado" />
        <StackPanel Grid.Row="1" Margin="0,5">
            <TextBlock
                FontSize="18"
                FontWeight="Bold"
                Text="Nombres" />
            <TextBox x:Name="NamesTextBox" FontSize="16" />
        </StackPanel>
        <StackPanel Grid.Row="2" Margin="0,5">
            <TextBlock
                FontSize="18"
                FontWeight="Bold"
                Text="Usuario" />
            <TextBox x:Name="UsernameTextBox" FontSize="16" />
        </StackPanel>
        <StackPanel Grid.Row="3" Margin="0,5">
            <TextBlock
                FontSize="18"
                FontWeight="Bold"
                Text="Apellidos" />
            <TextBox x:Name="LastnamesTextBox" FontSize="16" />
        </StackPanel>
        <StackPanel Grid.Row="4" Margin="0,5">
            <TextBlock
                FontSize="18"
                FontWeight="Bold"
                Text="Email" />
            <TextBox x:Name="EmailTextBox" FontSize="16" />
        </StackPanel>
        <Grid Grid.Row="5" Margin="0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="0,5,5,0">
                <TextBlock
                    FontSize="18"
                    FontWeight="Bold"
                    Text="Rol" />
                <Border
                    Padding="6,3"
                    Background="#D9D9D9"
                    CornerRadius="10">
                    <ComboBox
                        x:Name="RoleComboBox"
                        VerticalContentAlignment="Center"
                        Background="Transparent"
                        BorderThickness="0"
                        DisplayMemberPath="Display"
                        FontSize="14"
                        SelectedIndex="0"
                        SelectedValuePath="Value" />
                </Border>
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="5,5,0,0">
                <TextBlock
                    FontSize="18"
                    FontWeight="Bold"
                    Text="Telefono" />
                <TextBox x:Name="PhoneTextBox" FontSize="16" />
            </StackPanel>
        </Grid>
        <StackPanel
            Grid.Row="6"
            Margin="0,15,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Width="100"
                Margin="0,0,15,0"
                Background="#FF4D4D"
                Click="DeleteButton_Click"
                Content="Eliminar"
                Cursor="Hand"
                FontSize="18"
                Foreground="White" />
            <Button
                Width="100"
                Margin="0,0,15,0"
                Background="Transparent"
                Click="SaveButton_Click"
                Content="Guardar"
                Cursor="Hand"
                FontSize="18" />
            <Button
                Width="100"
                Background="Black"
                Click="CancelButton_Click"
                Content="Cancelar"
                Cursor="Hand"
                FontSize="18"
                Foreground="White" />
        </StackPanel>
    </Grid>
</Window>
